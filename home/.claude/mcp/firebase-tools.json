{
    "$schema": "../mcp-schema.json",
    "mcpServers": {
        "firebase": {
            "command": "npx",
            "args": [
                "-y",
                "firebase-tools@latest",
                "mcp",
                "--only",
                "crashlytics"
            ]
        }
    }
}
